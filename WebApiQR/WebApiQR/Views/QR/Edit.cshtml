@model WebApiQR.QR_CODE

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>QR_CODE</legend>

        @Html.HiddenFor(model => model.id)

        <div class="editor-label">
            @Html.LabelFor(model => model.qr_local)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.qr_local)
            @Html.ValidationMessageFor(model => model.qr_local)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.qr_name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.qr_name)
            @Html.ValidationMessageFor(model => model.qr_name)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
