@model WebApiQR.QR_CODE

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>QR_CODE</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.qr_local)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.qr_local)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.qr_name)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.qr_name)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
